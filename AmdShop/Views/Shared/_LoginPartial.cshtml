@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav   justify-content-center  justify-content-xxl-end ">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item ">
            <a id="manage" class="nav-link text-white hover " asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><i class="fa-solid fa-user icons"></i> Account</a>
        </li> 
          
        <li class="nav-item">
            <form id="logoutForm" class="form-inline " asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link btn btn-link text-white  hover ">Logout</button>
            </form>

        </li>

       @*  <li class="nav-item d-inline p-2">
            <img src="/images/dotnet.png" class="logoimg2 mr-2" />
            <img src="/images/js.png" class="logoimg2" />
        </li> *@
         
    }
    else
    {
        <li class="nav-item ">
            <a class="nav-link text-white hover " id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white hover " id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>

       @*  <li class="nav-item d-inline p-2">
            <img src="/images/dotnet.png" class="logoimg2 mr-2" />
            <img src="/images/js.png" class="logoimg2" />

        </li> *@
    }
</ul>
